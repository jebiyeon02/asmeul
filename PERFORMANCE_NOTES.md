# 몰입모드 및 공통 UI 성능 확인

## 2026-09-15: 메인 창 활성화 시 끊김

아래의 9월 14일 기록은 당시 구현과 측정입니다. 현재 소스에서는 중첩 `GlassEffectContainer`가 이미 제거되어 있었습니다. README의 컨테이너 설명도 현재 구현에 맞게 수정했습니다.

### 우선순위와 비교

사용자는 기본 화면에 포커스가 있을 때만 끊기며, 설정 창이 앞에 있거나 몰입 모드에서는 괜찮다고 보고했습니다. 이전의 배경 제거·개별 카드 유리 효과 제거는 개선이 없었고, 기본 UI 전체 제거는 개선됐습니다.

1. **다수의 기본 슬라이더 렌더링**: 창 활성화에 따라 달라지는 컨트롤 경로를 먼저 분리했습니다. 기본 화면의 음악 효과·트랙 음량·거리·출력 슬라이더를 `NSSlider`와 직접 그리는 `NSSliderCell`로 바꿨습니다. 카드, 사이드바, 하단 바의 `glassEffect`, 배경, 갱신 주기, 오디오 모델은 유지했습니다.
2. **유리 표면과 컨트롤의 조합**: 슬라이더 비교 빌드에 대해 사용자가 “지금 좋은데?”라고 개선을 확인했습니다. 따라서 추가로 유리 표면을 제거하지 않았습니다. 이 결과는 슬라이더 렌더링 경로를 유력하게 가리키지만, OS 내부의 특정 셰이더나 Liquid Glass만의 결함을 입증한 것은 아닙니다.
3. **음량계·모델 갱신**: 음량계는 기존처럼 별도 객체에서 최대 4Hz로 갱신하고, 표시가 같으면 알림을 보내지 않습니다. 정지 상태에서도 창 전환에 따른 차이가 관찰되어 이번 변경의 대상에서 제외했습니다.

### 최종 변경과 UI 영향

- 메인 화면에만 `MixerSlider`를 적용합니다. 몰입 모드는 기존 SwiftUI 슬라이더를 유지합니다.
- 범위, 바인딩, 연속 드래그, AppKit의 키보드·접근성 구현을 유지합니다. 매 SwiftUI 갱신마다 최신 바인딩을 coordinator에 전달하고, 값이 같은 경우 다시 쓰지 않습니다.
- 금색 막대, 밝은 손잡이, 16pt 높이와 기존 배치를 유지합니다. 손잡이의 동적인 유리 표현만 직접 그리는 표현으로 바뀝니다. 반복된 기본 컨트롤의 렌더링 경로를 피하기 위한 변경입니다.
- 재생 여부나 창 포커스에 따라 UI를 끄는 우회 처리를 추가하지 않았습니다.

### 검증과 측정 한계

- 소스와 일치하는 release 앱을 먼저 빌드하고, 슬라이더만 교체한 비교 앱을 실행했습니다. 사용자가 이 비교 앱에서 개선을 확인했습니다. 비교 앱은 몰입 슬라이더도 교체한 상태였으나, 최종 변경은 문제가 보고된 메인 화면으로 한정했습니다.
- 실제 앱의 접근성 값 설정으로 Space를 변경했을 때 수치와 모델이 함께 갱신됨을 확인하고 원래 값으로 복원했습니다. 사용자의 별도 트랙 음량 변경은 유지했습니다.
- `scripts/test.sh`: DSP, 믹서, HRTF, 오디오 라우팅, 음원 탐색, 애니메이션 시계, 음량계, 사진 디코딩·캐시 검사 통과.
- 최종 `scripts/build.sh`의 release 빌드·서명 검증 통과. `build/ASMEUL.app`과 배포 ZIP을 갱신하고 앱을 재실행하여 기존 선택 6개, 음악 효과 값, 사용자가 변경한 트랙 음량 및 원래 간격의 레이아웃을 확인했습니다. 키보드 방향키 검사는 실제 슬라이더 포커스를 확보하지 못해 성공 여부를 확정하지 않았습니다.
- 창 전환 전후 앱 CPU는 대체로 0–0.2%였습니다. WindowServer CPU는 관측 구간별 약 43–76%로 변동했고, 설정 창이 앞에 있는 구간도 변동했습니다. 다른 앱과 화면 캡처의 영향이 포함되어 있으므로 이 값을 개선율로 사용하지 않습니다.
- 설정 창 전면에서 수집한 Animation Hitches 기록의 디스플레이 렌더 구간은 689개, 중앙값 1.552ms, p95 2.872ms였습니다. 시스템 전체 디스플레이 합성 기록이며 ASMEUL 단독 GPU 시간이나 수정 전후 프레임 비교가 아닙니다. Mission Control의 정확한 fps 개선은 계측하지 않았습니다.
- 빌드 로그·관측 자료는 `.build/focus-analysis/`에 있습니다.

구현 근거: Apple의 [NSSliderCell](https://developer.apple.com/documentation/appkit/nsslidercell), [drawBar(inside:flipped:)](https://developer.apple.com/documentation/appkit/nsslidercell/1444587-drawbar), [drawKnob(_:)](https://developer.apple.com/documentation/appkit/nsslidercell/1444606-drawknob). Apple이 지원하는 셀 그리기 재정의를 사용하며 비공개 API나 시스템 설정 변경을 사용하지 않습니다.

---

2026-09-14, Apple Silicon / macOS 26.6.2, release 빌드에서 확인했습니다.

## 확인한 병목

기존 몰입 화면은 12Hz `TimelineView`가 사진의 변환과 명암 레이어 전체를 갱신하고 `drawingGroup`으로 합성했습니다. `NSImage(contentsOf:)` 캐시가 있어도 렌더링 중 JPEG의 지연 디코딩이 반복됐습니다.

4초 `sample` 프로파일의 메인 스레드 표본 2,902개 중 1,174개가 ImageIO의 이미지 표면 생성 경로, 1,170개가 `applejpeg_decode_image_all` 경로에 걸렸습니다. 이 수치는 호출 횟수가 아니라 해당 스택에서 수집한 표본 수입니다.

## 변경 후 확인

- `CGImageSourceCreateThumbnailAtIndex`와 즉시 캐시 옵션으로 actor 안에서 디코딩하고, 크기별 `CGImage`를 재사용합니다. 미리보기에는 원본 크기의 텍스처를 쓰지 않습니다.
- 사진은 `CALayer.contents`에 유지하고 확대·이동만 Core Animation으로 처리합니다. 명암 레이어는 파티클 시간 변화에 의존하지 않습니다.
- 파티클은 최대 30fps, 메인 화면 광원은 최대 15fps로 분리했습니다. Canvas는 이 화면에 불필요한 extended-linear 색상 처리를 하지 않습니다.
- 오디오 모니터의 250ms 갱신은 별도 음량계 객체에 전달하며, 표시되는 막대 수가 같으면 알림을 보내지 않습니다.

| 측정 | 변경 전 | 변경 후 |
| --- | --- | --- |
| `top` CPU 구간 | 61.3%, 93.3% | 4.1%, 8.4%, 8.9%, 7.3% |
| 몰입 사진 | 눈 내리는 카페 | 지구의 밤 |
| 선택된 소리 | 빗소리 1 | 빗소리 1, 심우주1, 심우주2 |
| 메인 스레드 JPEG 디코딩 표본 | 1,170 / 2,902 | 0 / 2,509 |

변경 후 3초 프로파일에서 메인 스레드 표본 2,371 / 2,509개는 이벤트 대기 경로였습니다. 사진과 오디오 설정 및 시스템 부하가 다른 관찰 구간이므로 CPU 수치를 동일 조건의 개선율로 해석하지 않습니다. 프레임 표시 시간을 직접 계측하지 않았으므로 일정의 목표 fps를 실측 fps로 주장하지 않습니다.

## 검증

- `swift build -c release`: 통과.
- `scripts/test.sh`: 기존 DSP·믹서·HRTF 검사 및 새 렌더링 회귀 검사 통과.
- 애니메이션 시계: 정지/재개, 중복 시작/정지, 긴 정지 후 재개, 10분 이후 시간 연속성.
- 음량계: 값이 같을 때 알림 억제, 감쇠, 정지 후 초기화.
- 환경 사진 5개: 512px/2560px 디코딩 상한, 크기별 캐시 구분, 동일 이미지 재사용, 없는 파일 처리.
- 실제 앱 재실행, 저장된 믹스 유지, 재생, 몰입모드 진입, 사진·빗줄기·컨트롤 표시 확인.

긴 오디오 파일은 기존과 같이 시작 시 전체 디코딩합니다. 이번 변경은 오디오 로딩 방식이나 음질을 바꾸지 않았으며, 큰 믹스의 오디오 메모리 사용량은 별도 항목입니다.

구현 시 참고한 Apple 문서: [이미지와 그래픽 처리](https://devstreaming-cdn.apple.com/videos/wwdc/2018/219mybpx95zm9x/219/219_image_and_graphics_best_practices.pdf), [애니메이션 일정](https://developer.apple.com/documentation/swiftui/timelineschedule/animation%28minimuminterval%3Apaused%3A%29).

## 후속 변경: 유리 효과 묶음과 60fps 파티클

2026-09-14, 위 측정 이후 적용했습니다.

- 메인 화면에 `AdaptiveGlassContainer`를 실제 연결했습니다. 사이드바·하단 바는 바깥 컨테이너, 트랙 카드는 `ScrollView` 내부의 별도 컨테이너에서 묶습니다. 효과 병합 임계값은 0으로 두었습니다.
- 바깥 컨테이너만 사용할 경우 스크롤한 카드가 헤더를 덮는 문제가 있어 카드용 컨테이너를 스크롤 안에 배치했습니다. 같은 스크롤 동작에서 헤더·하단 바 유지와 카드 클리핑을 확인했습니다.
- 메인·몰입모드 파티클의 목표 일정을 30→60fps로 변경했습니다. 느린 광원은 15fps를 유지하고, 호출부에서 갱신률을 명시하도록 했습니다. 정지·가림·동작 줄이기 조건은 기존대로 적용합니다.
- release 빌드, 앱 서명 검증과 배포 ZIP 생성 완료. 기존 렌더링 검사(시계 정지·재개, 음량계 알림, 사진 디코딩·캐시) 통과.
- 새 앱을 재실행해 저장된 3개 트랙 유지, 재생, 카드 스크롤, 몰입모드 진입·복귀와 정지를 확인했습니다.

60fps는 요청한 갱신 일정이며 실제 표시 fps를 계측한 결과는 아닙니다. 이번 변경으로 Mission Control 끊김이 해결됐다고 확정하지 않습니다.

## 추가 변경: 배경 60fps·파티클 120fps

사용자의 추가 요청으로 메인 배경 광원 목표 일정을 15→60fps, 메인·몰입모드 파티클을 60→120fps로 올렸습니다. 유리 효과 묶음과 정지·가림·동작 줄이기 조건은 유지합니다. 실제 표시 주기는 디스플레이와 시스템 스케줄링에 따릅니다.

release 빌드와 서명 검증을 통과했고 앱과 배포 ZIP을 갱신했습니다. 새 프로세스에서 기존 3개 트랙의 재생, 카드 스크롤과 클리핑, 몰입모드 진입·복귀, 정지를 확인했습니다.

동일한 3개 트랙을 재생한 짧은 `top` 관측(각 5회, 첫 누적 표본 제외)에서 앱 CPU는 변경 전 1.8–3.1%, 변경 후 1.7–5.5%였습니다. 원자료는 `.build/frame-analysis/before-120fps.top.txt`와 `after-120fps.top.txt`에 있습니다. 창 가시성·시스템 부하·애니메이션 위상을 완전히 통제한 벤치마크가 아니며, 이 수치로 GPU 비용이나 실제 120fps 유지 여부를 판단하지 않습니다. Mission Control의 프레임 개선은 아직 확정하지 않았습니다.

## 공간 음향 필터 계수 공유

2026-09-14. 작업 시작 시 존재하던 36개 슬롯 및 낙엽 음원 관련 변경을 보존한 상태에서 측정했습니다.

각 `Binaural`에 중복 저장하던 HRTF 계수를 출력 샘플레이트별로 믹서 안에서 한 번 생성하고, 36개 트랙이 읽기 전용으로 공유하도록 변경했습니다. 음원 디코딩용 엔진은 재생 준비를 하지 않으므로 계수 저장 공간도 할당하지 않습니다. 트랙별 히스토리·방향·거리·페이드 상태는 각각 유지하며 오디오 콜백에는 할당, 잠금, 참조 카운트 변경을 추가하지 않았습니다.

| 측정 | 변경 전 | 변경 후 |
| --- | --- | --- |
| 믹서 인라인 저장 공간 (`sizeof(Soundscape)`) | 1,846,464B | 298,752B |
| 준비 완료 시 계수 은행의 추가 힙 저장 공간 | 없음 | 43,016B + 공유 포인터 제어 블록 |
| 48/96kHz 교대 준비 100회, 5회 측정 중앙값 | 9.88ms | 0.72ms |
| 동일한 전체 DSP 오프라인 작업, 3회 경과 시간 중앙값 | 2.30s | 1.60s |

준비된 믹서의 고정 상태와 필터 저장 공간은 약 1.76MiB에서 0.33MiB로 감소했습니다. 음원 PCM, 리버브 버퍼, 앱 전체 메모리를 포함한 수치는 아닙니다. 시간 수치는 로컬 마이크로벤치마크 결과이며 실제 앱 전체 CPU 사용률이나 오디오 지연 개선율을 뜻하지 않습니다.

전체 DSP 비교는 36개 슬롯에 서로 다른 소스를 넣고 44.1/48/96/192kHz마다 65,536 프레임씩 처리했습니다. 256프레임마다 파라미터를 반영하며 트랙 음량, 방향, 거리, 공간 음향 켜기·끄기와 루프 전환을 포함했습니다. 변경 전후 2MiB 출력은 바이트 단위로 같았으며 SHA-256은 `9eb14e0cc0db8e40234e43a7008ac18fa5fcc0ecd5c4f9125db6d196bbb06865`입니다. 측정 소스와 원자료는 `.build/optimization-baseline/`에 남겼습니다.

기존 개별 공간화기 벤치마크는 각 인스턴스가 독립 계수를 만드는 경로에서 24트랙 중앙값이 45.64→52.09ms로 증가했습니다. 실제 믹서는 계수를 공유하므로, 재사용 가능한 벤치마크도 같은 준비 방식을 사용하도록 수정했습니다. 위 전체 DSP 비교는 실제 `Soundscape`의 공유 경로를 검증합니다.

검증:

- `scripts/test.sh`: DSP, 믹싱, HRTF, 라우팅, 음원 탐색, 렌더링 검사 통과.
- 공유 계수의 수명, 트랙 상태 독립성, 일부 트랙의 샘플레이트 재설정, 4가지 샘플레이트에서 독립 계수와 출력 일치 검사 추가 및 통과.
- 기존 스칼라 HRTF 기준 대비 최대 오차 `3.57628e-07`로 기존과 동일.
- `BinauralTests`와 `SoundLifetimeTests`: AddressSanitizer/UndefinedBehaviorSanitizer 통과. 기본 Xcode Clang 17의 ASan은 `main` 진입 전 초기화 잠금에서 멈춰 중단했고, 설치된 Command Line Tools Clang 21에 Xcode SDK와 C++ 헤더를 지정하여 검사했습니다. 따라서 기본 `scripts/test-performance.sh` 전체 실행이 성공한 것으로 기록하지 않습니다.
- 로딩 검사의 Swift `-O`가 `assert` 및 그 안의 음원 설치 호출을 생략하는 것을 확인해 `-Onone`으로 변경했습니다. 검증 활성화 상태에서 내장 25개 음원 디코딩·설치·렌더링·해제, 취소 및 MP3 가져오기 검사를 통과했습니다. 총 검증 PCM은 588,556,560B입니다.
- `scripts/build.sh`: release 빌드, 앱 서명 검증, 앱 및 배포 ZIP 갱신 완료. 실제 출력 장치를 통한 청취 검사는 이번 변경에서 수행하지 않았습니다.
